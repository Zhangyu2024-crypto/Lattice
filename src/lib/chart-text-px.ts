/**
 * Numeric font sizes for ECharts / canvas APIs that cannot use `var(--text-*)`.
 * Values match `tokens.css` --text-* scale.
 */
export const CHART_TEXT_PX = {
  micro: 8,
  '2xs': 9,
  xxs: 10,
  xs: 11,
  sm: 12,
  base: 13,
  md: 14,
  lg: 16,
  xl: 18,
  '2xl': 22,
  '3xl': 26,
} as const
