// Default sizes for the Pro Workbench shell panels. Kept separate so
// the main shell component and any helper modules can import them
// without pulling in the whole component file.

export const DEFAULT_INSPECTOR_WIDTH = 320
export const DEFAULT_INSPECTOR_MIN = 220
export const DEFAULT_INSPECTOR_MAX = 520

export const DEFAULT_DATA_TABS_HEIGHT = 280
export const DEFAULT_DATA_TABS_MIN = 120
export const DEFAULT_DATA_TABS_MAX = 600

export const RAIL_COLLAPSED_WIDTH = 28
export const RAIL_EXPANDED_WIDTH = 220
