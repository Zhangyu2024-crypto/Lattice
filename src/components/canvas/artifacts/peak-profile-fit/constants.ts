// Chart colour tokens for the single-peak pseudo-Voigt profile fitter.
// Kept as CSS variables so the modal follows the global theme tokens
// without hard-coding saturated fallbacks.

export const CHART_COLOR_DATA = 'var(--color-text-primary)'
export const CHART_COLOR_MODEL = 'var(--color-accent)'
